import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";
import Navbar from "./components/navbar";
import Footer from "./components/Footer";
import { Noto_Sans, Titillium_Web } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });
const popins = Poppins({weight:["100","200","400"], subsets:["latin"]  });
const titi = Titillium_Web({weight:["200","400", "700"], subsets:["latin"]});
export const metadata: Metadata = {
  title: "Hexa Automation",
  description: "Generated by DiArMo dev",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body className={titi.className}>
        <Navbar />
        {children}
        <Footer />
        </body>
    </html>
  );
}
